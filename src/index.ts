import Requester from './requester'
import {
  dataExtract,
  errorExtract,
  loader, LoaderOpts,
  dedupe, DedupeOpts,
} from './middlewares'

export {
  Requester,

  dataExtract,
  errorExtract,
  loader, LoaderOpts,
  dedupe, DedupeOpts,
}
